# Lo que necesita la app para RODAR (Mac o Windows):
#
#     pip install -r requirements.txt
#
# Las versiones son minimos, no clavos: son las validadas en pista y en los
# tests. El salto de pandas 2.3 a 3.0 y de numpy 2.0 a 2.4 no movio ni un
# aviso, asi que no hay motivo para congelar por miedo. Si algun dia una
# version nueva rompe un golden, el test canta y ese es el momento de fijarla.
numpy>=2.0
pandas>=2.3
sounddevice>=0.5          # audio de latencia minima (arrastra PortAudio)
miniaudio>=1.71           # decodifica los clips de voz sin depender de ffmpeg
customtkinter>=6.0        # la ventana (gui.py)

# Telemetria en vivo de iRacing. Solo Windows: en el Mac no hay simulador y
# source.py lo importa tarde, asi que el resto de la app funciona sin el.
pyirsdk>=1.3.5; sys_platform == "win32"
