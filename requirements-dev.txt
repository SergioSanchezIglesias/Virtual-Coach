# Lo que hace falta para DESARROLLAR y para CONSTRUIR el .exe, ademas de lo
# de runtime:
#
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Van aparte a proposito: nada de esto viaja en el ejecutable ni hace falta
# para rodar. pytest no lo necesita el coach, y edge-tts solo se usa para
# REGENERAR los clips de voz (que se versionan ya hechos en voces/).
pytest>=8.0
pyinstaller>=6.0          # construir el .exe, EN WINDOWS
edge-tts>=7.0             # regenerar voces/ con gen_voces.py
